# Dependencies
mobile/node_modules/

# React Native
mobile/.metro-health-check*
mobile/android/app/build/
mobile/ios/build/

# Java / Maven (backend)
backend/target/

# Logs de runtime de Quarkus dev (ruido generado, no es código)
backend/quarkus-run.log
backend/quarkus-run.err
backend/quarkus-run.log.err

# Instalador OpenCode (accidental, no es código del proyecto)
install.ps1

# Herramienta de actualización Java (plan/ruido generado, no es código del proyecto)
backend/.github/

# IDE (user-specific)
mobile/.vscode/

# OS
.DS_Store
Thumbs.db

# Office lock files (temporales)
~$*

# Environment
mobile/.env
mobile/.env.local
.env

# Keystore de firma de produccion (NUNCA subir a git) - H9
mobile/android/keystore.properties
mobile/android/app/insectos-beneficos-release.keystore

# Credenciales Firebase (service account admin SDK = SECRETO, NUNCA subir a git)
_firebase/

# Uploads runtime (volumen Docker, no versionar)
backend/uploads/

# Paquete de despliegue (JAR pre-compilado + secrets, no versionar)
deploy-package/
