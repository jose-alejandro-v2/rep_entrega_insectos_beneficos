# HITO-004 — Módulo Programación de Stock (listado, edición y creación)

## Estado

**CERRADO** — 2026-08-21. Implementación completada y verificada. Se completó el
módulo Programación: listado por mes, edición con restricción de días (RF-147/148)
y creación de programaciones desde la app (botón "Nuevo"). Incluye el endpoint
backend `POST /api/v1/programaciones` con RBAC y la migración V4.

## Alcance implementado

- **Commit `80b271f` (mobile)**: pantallas Programación — Screen 4 (listado por
  mes con selector de periodo, `ProgramacionScreen`) y Screen 5 (edición,
  `ProgramacionEdicionScreen`), `ApiClient` extendido, helpers de fechas/día
  editable, navegación, tests (61/61 en su momento).
- **Commit `cd3ef0f` (backend)**: paquete `programacion` — `Especie`,
  `EspecieResource/Service`, `Programacion`, `DetalleProgramacion`,
  `ProgramacionResource`, `ProgramacionService`, `ProgramacionMapper`,
  DTOs, repositorios Panache y migración `V4__create_programaciones.sql`.

## Delta de cierre (working tree, commit único)

- **Backend**: `programacion/dto/CrearProgramacionRequest.java` (nuevo, con
  Bean Validation `@NotNull/@Positive` y `@Min/@Max` en mes); `ProgramacionResource`
  (POST crear con `@Valid` + `@RolesAllowed`; RBAC extendido a PUT y
  POST /publicar); `ProgramacionService` (método `crearProgramacion` con 404
  especie y 409 duplicado, reutiliza `crearProgramacionInicial`).
- **Mobile**: `ApiClient` (`CrearProgramacionRequest` + `crearProgramacion`);
  `navigation/types.ts` (union type `ProgramacionEdicion` crear/editar);
  `ProgramacionScreen` (botón "Nuevo"); `ProgramacionEdicionScreen` (modo
  crear/editar con `navigation.replace`); tests del botón "Nuevo" y del flujo crear.
- **Docs/versión**: `04_implementacion.md` §33; `OPENCode_orquestacion_agentes_proyecto_v2.md`
  §25.1/§25.1.1 (hot reload + pipeline dev→auditor); bump **1.3.0 / versionCode 4**
  en `package.json`, `appVersion.ts`, `build.gradle` y entrada `1.3.0` en `versionHistory.js`.

## Evidencia final (get review → re-auditoría PASS)

- **Gates evaluados**: G-API (POST 201), G-SEC/RBAC (401 sin token en POST/PUT/publicar),
  G-ORM/G-TX, G-VAL (input vacío → 400), G-MOB-NAV/FORM/UI, G-TEST-BE, G-TEST-FE,
  G-DOC/G-DOC-SYNC, G-EFF (DRY reutiliza `crearProgramacionInicial`), G-NOTRIAL/G-ANAL.
- **Hallazgos**: 0 CRÍTICO, 0 ALTO. Los MEDIO (H1 validación, H3/H4 tests,
  H5 doc-sync, H6 RBAC) fueron resueltos antes del cierre. Deuda registrada:
  **H8 (BAJO)** — form de creación sin RHF+Zod (aceptado con plan de deuda).

## Verificación de cierre (re-ejecutada por el auditor)

- Backend `.\mvnw.cmd clean package -DskipTests`: **BUILD SUCCESS**.
- Backend `.\mvnw.cmd test`: **Tests run: 39, Failures: 0, Errors: 0** (13 Auth +
  7 Programación + 19 Usuarios).
- Mobile `npm run lint`: **PASS** (exit 0).
- Mobile `npx tsc --noEmit`: **PASS** (exit 0).
- Mobile `npx jest --runInBand`: **PASS — 12 suites / 63 tests**.
- End-to-end (backend vivo puerto 6113): `GET /especies` 200; `POST /programaciones`
  201/409/404/400/401; `PUT /programaciones/1` sin token 401; `POST /programaciones/1/publicar`
  sin token 401.
- Auditoría integral de cierre: **PASS** (0 crít./0 altos; MEDIO resueltos; deuda H8 BAJO registrada).
- Commit único de cierre ejecutado por el Orchestrator (excluyendo `install.ps1`).
