/**
 * appVersion.ts — Versión visible del aplicativo mobile (HITO-003, Ley 3).
 *
 * Debe coincidir EXACTAMENTE con `package.json` (version 1.14.4),
 * `android/app/build.gradle` (versionName 1.14.4 / versionCode 21) y con la
 * entrada `1.14.4` de `versionHistory`. Se expone en PerfilScreen.
 */
export const APP_VERSION = '1.14.4';